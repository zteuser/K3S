apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-sharedata1-additional
  labels:
    type: ocfs2
    storage: sharedata1
spec:
  capacity:
    storage: 45Gi
  accessModes:
    - ReadWriteMany  # OCFS2 підтримує одночасний доступ з кількох нод
  persistentVolumeReclaimPolicy: Retain
  storageClassName: ocfs2-shared
  hostPath:
    path: /sharedata1
    type: DirectoryOrCreate
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - master-node
          - work-node
