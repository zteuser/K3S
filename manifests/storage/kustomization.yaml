apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - storageclass-ocfs2.yaml
  - persistentvolume-sharedata1.yaml
  - persistentvolume-sharedata2.yaml

# Опціональні ресурси (розкоментуйте якщо потрібно)
# - persistentvolumeclaim-example.yaml
# - statefulset-example.yaml
# - deployment-example.yaml

commonLabels:
  managed-by: kustomize
  storage-type: ocfs2

