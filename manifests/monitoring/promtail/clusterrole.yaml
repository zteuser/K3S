# Мінімальні права для Promtail: discovery подів і нод (kubernetes_sd_config)
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: promtail
  labels:
    app: promtail
rules:
- apiGroups: [""]
  resources:
  - pods
  - nodes
  verbs: ["list", "watch", "get"]
